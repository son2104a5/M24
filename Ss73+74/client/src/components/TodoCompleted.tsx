import React from 'react';
import TodoList from './TodoList';

interface Todo {
  id: number;
  name: string;
  completed: boolean;
}



export default function TodoCompleted() {
  // const completedTodos = state.filter(todo => todo.completed);
  return (
    // <TodoList state={completedTodos} dispatch={dispatch} handleEditTask={handleEditTask} />
    <div></div>
  );
}
